rules_version='2'

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Admin SDK bypasses these rules; client writes require authentication
      allow read, write: if request.auth != null;
    }
  }
}
